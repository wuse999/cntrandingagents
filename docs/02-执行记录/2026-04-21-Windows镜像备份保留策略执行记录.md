# 2026-04-21 Windows 镜像备份保留策略执行记录

## 1. 背景

- 本轮任务来源：为 Windows 本地镜像补一套可复用的备份保留与自动清理策略。
- 当前目标：固定“默认保留最近 3 份完整快照”的规则，并提供可直接执行的清理脚本。

## 2. 前置检查

- 执行环境：Windows 本地镜像工作区
- 当前工作树：`D:\tranding\tranding_agents_cn\cntrandingagents`
- `git status -sb`：开工前本地镜像已与 `origin/main` 对齐
- `git remote -v`：已存在 `origin`、`gitee`、`upstream`
- 编码检查：文档统一按 UTF-8 读取
- 是否需要切换 Ubuntu 正式主工作树：本轮先在 Windows 落地规则与脚本，正式提交阶段再回 Ubuntu

## 3. 本次改动范围

- 修改目录：
  - `docs/01-项目约定`
  - `docs/02-执行记录`
  - `docs/superpowers`
  - `tools`
- 修改文件：
  - 1 份长期规则文档
  - 1 份 PowerShell 清理脚本
  - 1 份执行记录
  - 1 份设计文档
  - 1 份计划文档
  - 4 份索引/检查清单/台账更新
- 是否只涉及汉化：否，属于治理文档与辅助脚本落地
- 是否涉及上游同步：否

## 4. 具体动作

1. 新增 Windows 镜像备份保留策略文档，固定路径、前缀、完整快照判定和默认保留数量。
2. 新增 `tools/cleanup_windows_mirror_backups.ps1`，支持 `-WhatIf`、默认保留 3 份、跳过不完整快照。
3. 更新总索引、项目约定 README、AI 自动执行清单与长期踩坑台账。
4. 用 `-WhatIf` 和临时测试根目录验证脚本行为，并回填结果。

## 5. 验证结果

- `powershell -ExecutionPolicy Bypass -File .\tools\cleanup_windows_mirror_backups.ps1 -WhatIf`：
  - 已执行。
  - 当前真实备份根目录 `_bootstrap_backup` 下共有 `1` 份完整快照 `windows-mirror-align-20260421-142550`。
  - 结果为“保留 1 份，不删除任何目录”。
- 临时测试根目录验证：
  - 已在 `D:\tranding\tranding_agents_cn\_tmp_backup_retention_test` 构造 `4` 份完整快照和 `1` 份不完整快照。
  - `-WhatIf` 演练结果：保留 `140004 / 140003 / 140002`，计划删除最旧的 `140001`，跳过不完整目录 `140005`。
  - 正式执行结果：成功删除 `140001`，剩余 `140002 / 140003 / 140004 / 140005`，其中 `140005` 仍被保留用于人工复核。
- 文档检查：
  - 已新增长期规则文档、设计文档、计划文档与执行记录，并补齐总索引、README、AI 检查清单与长期台账入口。
- UTF-8 检查：
  - 已使用 `Get-Content -Encoding UTF8` 抽检文档内容，未见新增乱码。
- CLI 或运行验证：
  - 本轮不涉及 CLI

## 6. 新增踩坑与处理

- 本轮新增踩坑：
  - Windows 本地镜像每次对齐前都做完整快照，若无保留策略，`_bootstrap_backup` 会持续膨胀。
- 临时处理：
  - 本轮直接固化为长期规则与脚本。
- 是否已补入 `00-长期踩坑与经验回收.md`：
  - 是

## 7. 可沉淀经验/规则回写

- 本轮形成的长期经验：
  - Windows 镜像对齐备份必须纳入默认保留策略，且只清理完整快照。
- 是否回写 `docs/01-项目约定`：
  - 是
- 是否需要更新索引或 README：
  - 是

## 8. 后续待办

- 在 Ubuntu 正式主工作树提交并推送本轮治理文档与脚本。
- 后续若要完全自动化，可再单开一轮把脚本接入 Windows 计划任务。

## 9. 本次结论

- 已为 Windows 本地镜像补齐“默认保留最近 3 份完整快照”的长期规则，并提供可直接执行的 PowerShell 清理脚本。
- 脚本在真实目录的 `-WhatIf` 演练和临时测试目录的实际删除验证都已通过。
