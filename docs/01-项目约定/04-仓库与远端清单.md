# 仓库与远端清单

## 1. 文档用途

- 统一记录 `cntrandingagents` 项目当前所有关键路径、远端和默认分支。
- 作为后续初始化、排障和验证的基线。

## 2. 本地路径清单

### 2.1 Windows 上游镜像路径

- `D:\tranding\tranding_agents_cn\raw`

### 2.2 Windows 汉化仓路径

- `D:\tranding\tranding_agents_cn\cntrandingagents`

## 3. Ubuntu 路径清单

### 3.1 Ubuntu 正式主工作树

- `~/projects/cntrandingagents`

### 3.2 Ubuntu 角色

- 默认编辑面
- 默认 Git 执行面
- 默认同步面
- 默认发布面

## 4. 仓库远端清单

### 4.1 `upstream`

- `git@github.com:TauricResearch/TradingAgents.git`

### 4.2 `origin`

- `git@github.com:wuse999/cntrandingagents.git`

### 4.3 `gitee`

- `git@gitee.com:frpnat/cntrandingagents.git`

## 5. 默认分支与命名约定

- 默认主分支：`main`
- 上游同步分支建议：`sync/upstream-YYYYMMDD` 或 `sync/<official-tag>`
- 汉化整理分支建议：`l10n/<topic>`
- 发布 tag 建议：`vX.Y.Z-cntrandingagents.N`

## 6. 默认 Git 角色说明

- `upstream`：官方同步源
- `origin`：GitHub 主仓
- `gitee`：Gitee 镜像仓

## 7. 首次检查命令

在 Ubuntu 主工作树执行：

```bash
pwd
git branch --show-current
git remote -v
git status --short --branch
git ls-remote origin refs/heads/main
git ls-remote gitee refs/heads/main
git ls-remote upstream refs/heads/main
```

在 Windows 本地镜像执行：

```powershell
git -C D:\tranding\tranding_agents_cn\cntrandingagents status --short --branch
git -C D:\tranding\tranding_agents_cn\cntrandingagents remote -v
```

## 8. 当前基线状态

- `raw` 已接成 Git 仓库，当前跟踪官方 `origin/main`
- `cntrandingagents` 当前用于承载正式汉化文档与后续汉化主线
- GitHub 与 Gitee 规划沿用 `cnfrp` 的同一套账号体系与自动登录方式

## 9. 需要人工确认的项

- GitHub `wuse999/cntrandingagents` 是否已创建
- Gitee `frpnat/cntrandingagents` 是否已创建
- Ubuntu `~/projects/cntrandingagents` 是否已初始化
- 本地与 Ubuntu 是否都已挂好同名远端

## 10. 一句话结论

后续凡是涉及路径、远端、分支和角色判断，都以本清单为当前基线。
